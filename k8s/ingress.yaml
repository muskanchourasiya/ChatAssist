apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: chatassist-ingress
spec:
  rules:
    - host: chatassist.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: chatassist-service
                port:
                  number: 80